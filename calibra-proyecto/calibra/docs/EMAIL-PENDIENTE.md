# Pendiente: plantilla de correo personalizada

`email-template-magic-link.html` está listo pero **todavía no se aplicó**
en Supabase (Authentication → Email Templates → Magic Link) — falta
resolver el envío por SMTP propio antes de configurar esto, así que por
ahora la app sigue usando el correo de magic link genérico por defecto
de Supabase. Retomar cuando el SMTP esté andando.
